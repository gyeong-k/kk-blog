# GET이란?

- 작은 실습
  - console.log(req.method, req.url);
  - 브라우저에서 /와 /todos에 접속
  - [GET /] , [GET /todos] 출력
 
- GET : 브라우저가 “이 주소의 내용을 가져와 주세요”라고 요청하는 방식
  - 보통 데이터를 읽어올 때
    - 목록 보기
    - 페이지 보기
    - 정보 조회

## 작은 개념 더하기
- 라우팅은 기본 형태
  - 어떤 요청(method + url)이 왔는지 보고, 그에 맞는 응답을 보낸다.
